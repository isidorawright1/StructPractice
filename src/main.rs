//import libraries/modules
use std::io;

//define struct
struct Student {
    name: String,
    grades: Vec<u32>,
    id: u32,
}

fn main() {

}

//Tests
//tes to add a new student
//remove a student
//check passing grade
//check failing grade
#[cfg(test)]

mod tests {
    use super::*;
}